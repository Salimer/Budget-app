<div class="navigation py-6 px-3 blue-nav-white-text">
  <ul class="flex justify-between items-center">
    <li class="w-10"><%= link_to image_tag('backarrow.png', alt: 'Back'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'nav-link' %></li>
    <h2><strong>Categories</strong></h2>
    <li><%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %></li>
  </ul>
</div>
<section class="px-5">
  <p style="color: green"><%= notice %></p>

  <div id="categories">
    <% @categories.each do |category| %>
      <a href="<%= category_path(category) %>" class="m-1">
      <%= render category %>
      </a>
    <% end %>
  </div>
  <a href="<%= new_category_path %>" class="">
  <button class="p-2 green-btn rounded drop-shadow-md">New +</button>
  </a>
</section>
